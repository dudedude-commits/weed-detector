opencv-python-headless>=4.8
numpy>=1.24
scikit-image>=0.21
scikit-learn>=1.3
pandas>=2.0
Pillow>=10.0
streamlit>=1.30
altair>=5.0

# Всё это требует Python 3.9 или новее (рекомендуется 3.10-3.12).
# На Python 3.7/3.8 pip не найдёт подходящих версий numpy/streamlit —
# см. раздел "Если start.bat не запускается" в README.md.
